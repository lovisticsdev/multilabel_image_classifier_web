# Frontend

Run the React app:

```bash
cd frontend
npm install
npm run dev
```

The UI uploads an image to the FastAPI backend and displays predicted labels, top classes, confidence bars, and model metadata.
